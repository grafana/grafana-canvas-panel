services:
  grafana:
    user: root
    container_name: 'grafana-canvas-panel'

    build:
      context: .
      dockerfile: Dockerfile
      args:
        grafana_image: ${GRAFANA_IMAGE:-grafana-enterprise}
        grafana_version: ${GRAFANA_VERSION:-13.1.0-25893932881}
        development: ${DEVELOPMENT:-false}
        anonymous_auth_enabled: ${ANONYMOUS_AUTH_ENABLED:-true}
    ports:
      - 3000:3000/tcp
    volumes:
      - ../dist:/var/lib/grafana/plugins/canvas
      - ../provisioning:/etc/grafana/provisioning
      - ..:/root/grafana-canvas-panel

    environment:
      NODE_ENV: development
      GF_LOG_FILTERS: plugin.canvas:debug
      GF_LOG_LEVEL: debug
      GF_DATAPROXY_LOGGING: 1
      GF_PLUGINS_ALLOW_LOADING_UNSIGNED_PLUGINS: canvas

    healthcheck:
      test: ['CMD-SHELL', 'wget --no-verbose --tries=1 --spider http://localhost:3000/api/health || exit 1']
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 30s
